# Explainer render service — VPS env
# Bearer token required on POST /api/render (the caller sends Authorization: Bearer <token>).
EXPLAINER_RENDER_TOKEN=change-me-long-random

# Optional: default Kokoro voice (af_heart=warm female, am_onyx=deep male, etc.)
EXPLAINER_VOICE=af_heart
